#include "main.h"
/**
 *  _strlen - the length of a string
 *  @s: pointer to the string to get the length
 *  Return: int
 */
int _strlen(char *s)
{
	int i;
	
	for (i = 0; *(s+i) != '\0'; i++)
	{
	}

	return (i);
}
